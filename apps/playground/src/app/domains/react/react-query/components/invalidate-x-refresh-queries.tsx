// TODO
// 1. Create examples for invalidateQueries and refetchQueries
// 2. Create examples for Filtering Queries when invalidating them
/*
  const { data } = useQuery({
    queryKey: ['passing-arguments', userId],
    queryFn: fetchDynamicUserRecommendedWay,
  });

  const { data: data2 } = useQuery({
    queryKey: ['passing-arguments', userId, 'lorem'],
    queryFn: fetchDynamicUserRecommendedWay,
  });

    const { data: data3 } = useQuery({
    queryKey: ['passing-arguments',  userId, 'lorem', "ipsum"],
    queryFn: fetchDynamicUserRecommendedWay,
  });

  Create examples to invalidate:
  * only the first query
  * only the second query
  * both first and second query
  * all queries
  * only the third query

*/
